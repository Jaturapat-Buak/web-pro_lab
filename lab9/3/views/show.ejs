<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <title>ระบบจัดการคำสั่งซื้อ</title>
</head>
<body class="bg-light">

<div class="container mt-5">

    <h2 class="mb-4 text-center">📦 ระบบจัดการคำสั่งซื้อ</h2>

    <!-- ฟอร์มเพิ่ม -->
    <div class="card shadow mb-4">
        <div class="card-header bg-primary text-white">
            เพิ่มคำสั่งซื้อ
        </div>

        <div class="card-body">
            <form method="POST" action="/add" class="row g-3">
                <div class="col-md-3">
                    <input name="customer" class="form-control" placeholder="ชื่อลูกค้า" required>
                </div>

                <div class="col-md-3">
                    <input name="product" class="form-control" placeholder="สินค้า" required>
                </div>

                <div class="col-md-3">
                    <input name="address" class="form-control" placeholder="ที่อยู่" required>
                </div>

                <div class="col-md-2">
                    <input name="phone" class="form-control" placeholder="เบอร์โทร" required>
                </div>

                <div class="col-md-1">
                    <button class="btn btn-success w-100">เพิ่ม</button>
                </div>
            </form>
        </div>
    </div>

    <!-- ตาราง -->
    <div class="card shadow">
        <div class="card-header bg-dark text-white">
            รายการคำสั่งซื้อ
        </div>

        <div class="card-body table-responsive">

            <table class="table table-bordered table-hover align-middle text-center">
                <thead class="table-secondary">
                    <tr>
                        <th>ID</th>
                        <th>ลูกค้า</th>
                        <th>สินค้า</th>
                        <th>ที่อยู่</th>
                        <th>เบอร์โทร</th>
                        <th>สถานะ</th>
                        <th>เปลี่ยนสถานะ</th>
                    </tr>
                </thead>

                <tbody>
                <% data.forEach(o => { %>
                <tr>
                    <td><%= o.ID %></td>
                    <td><%= o.ลูกค้า %></td>
                    <td><%= o.สินค้า %></td>
                    <td><%= o.ที่อยู่ %></td>
                    <td><%= o.เบอร์โทร %></td>

                    <td>
                        <% if(o.สถานะ === "จัดส่งแล้ว") { %>
                            <span class="badge bg-success">จัดส่งแล้ว</span>
                        <% } else if(o.สถานะ === "กำลังจัดส่ง") { %>
                            <span class="badge bg-warning text-dark">กำลังจัดส่ง</span>
                        <% } else { %>
                            <span class="badge bg-secondary">รอดำเนินการ</span>
                        <% } %>
                    </td>

                    <td>
                        <form method="POST" action="/update/<%= o.ID %>" class="d-flex gap-2">
                            <select name="status" class="form-select form-select-sm">
                                <option value="รอดำเนินการ">รอดำเนินการ</option>
                                <option value="กำลังจัดส่ง">กำลังจัดส่ง</option>
                                <option value="จัดส่งแล้ว">จัดส่งแล้ว</option>
                            </select>
                            <button class="btn btn-primary btn-sm">อัปเดต</button>
                        </form>
                    </td>
                </tr>
                <% }) %>
                </tbody>

            </table>

        </div>
    </div>
</div>

</body>
</html>
